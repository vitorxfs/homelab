# Home Assistant configuration

http:
  use_x_forwarded_for: true
  trusted_proxies:
    - 127.0.0.1
    - ::1
    - 172.16.0.0/12   # Docker bridge networks (cloudflared container)
    - 192.168.0.0/24  # Local network

# Load default integrations
default_config:
